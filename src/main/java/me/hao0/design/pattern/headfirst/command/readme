命令模式(Command):
-----------------------定	义-----------------------
将请求封装成对象，以便使用不同的请求，队列，日志来参数化其他对象，命令模式也支持可撤销的操作。

-----------------------描	述-----------------------
*将方法调用封装起来.
*将发出请求的对象与接受请求的对象分离开来.