适配器模式(Adapter):
-------------------------定	义------------------------
将一个类的接口，转换成客户端期望的另一个接口，适配器让原本接口不兼容的类可以合作无间。

对象适配器：

通过组合的形式，将目标接口的调用委托给被适配者的属性对象来实现

类适配：
通过多继承(java不支持)的形式，将目标调用转换为被适配者调用
-------------------------描	述------------------------
java中的Enumeration

结构型模式
